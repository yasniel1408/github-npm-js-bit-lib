"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.computePrettierConfig = void 0;
const missing_prettier_config_1 = require("./exceptions/missing-prettier-config");
function computePrettierConfig({ configPath, config, }) {
    if (!config && !configPath) {
        throw new missing_prettier_config_1.MissingPrettierConfig();
    }
    if (config)
        return config;
    const prettierConfig = configPath
        // eslint-disable-next-line import/no-dynamic-require, global-require
        ? require(configPath)
        : undefined;
    return prettierConfig;
}
exports.computePrettierConfig = computePrettierConfig;
//# sourceMappingURL=get-prettier-config.js.map