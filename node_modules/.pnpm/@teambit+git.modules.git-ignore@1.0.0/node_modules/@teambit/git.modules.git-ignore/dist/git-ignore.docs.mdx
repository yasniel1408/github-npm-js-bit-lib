---
description: Manage git ignore file
labels: ['modules', 'node', 'git', 'git-ignore']
---

This component will manage git ignore file.
Right now it's used to generate `.gitignore` file in the root of the project.

## Usage

```js
import { gitIgnoreTemplate } from '@teambit/git.modules.git-ignore';
return gitIgnoreTemplate();
```
