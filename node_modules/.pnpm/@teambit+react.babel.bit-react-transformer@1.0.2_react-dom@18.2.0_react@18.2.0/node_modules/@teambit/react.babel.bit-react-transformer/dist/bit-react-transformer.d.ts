import type { PluginObj, PluginPass } from '@babel/core';
import type * as Types from '@babel/types';
export declare type BitReactTransformerOptions = {
    componentFilesPath?: string;
};
declare type Api = {
    types: typeof Types;
};
/**
 * the bit babel transformer adds a `componentId` property on React components
 * for showcase and debugging purposes.
 */
export declare function createBitReactTransformer(api: Api, opts: BitReactTransformerOptions): PluginObj<PluginPass>;
export {};
