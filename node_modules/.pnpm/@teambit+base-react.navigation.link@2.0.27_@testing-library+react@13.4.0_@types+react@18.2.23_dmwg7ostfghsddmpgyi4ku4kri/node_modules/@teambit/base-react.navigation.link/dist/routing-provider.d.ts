import React, { ReactNode } from 'react';
import { LinkType, UseLocation } from './link.type';
export declare type RouterContextType = {
    /**
     * link implementation.
     */
    Link?: LinkType;
    /**
     * useLocation implementation.
     */
    useLocation?: UseLocation;
};
export declare const RouterContext: React.Context<RouterContextType>;
/**
 * Gets routing components from context.
 * (defaults to native components)
 */
export declare function useRouter(): RouterContextType;
export declare function RouterProvider({ children, implementation, }: {
    children: ReactNode;
    implementation: RouterContextType;
}): JSX.Element;
