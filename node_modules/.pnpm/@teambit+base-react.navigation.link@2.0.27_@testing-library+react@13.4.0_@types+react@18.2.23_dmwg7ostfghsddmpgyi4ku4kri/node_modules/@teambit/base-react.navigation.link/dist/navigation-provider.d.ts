import React, { ReactNode } from 'react';
import { LinkType, UseLocation, UseNavigate } from './link.type';
export declare type RouterContextType = {
    /**
     * link implementation.
     */
    Link?: LinkType;
    /**
     * useLocation implementation.
     */
    useLocation?: UseLocation;
    /**
     * navigate to another page
     */
    useNavigate?: UseNavigate;
};
export declare const NavigationContext: React.Context<RouterContextType>;
/**
 * Gets routing components from context.
 * (defaults to native components)
 */
export declare function useNavigation(): RouterContextType;
export declare function NavigationProvider({ children, implementation, }: {
    children: ReactNode;
    implementation: RouterContextType;
}): JSX.Element;
